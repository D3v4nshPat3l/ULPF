# Per-installation secrets, generated by:
#   docker compose -f generate-indexer-certs.yml run --rm generator
# Committing them would ship one machine's private keys to everyone.
config/wazuh_indexer_ssl_certs/
